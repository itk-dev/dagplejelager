# Dagplejelager theme

```sh
docker-compose run node yarn --cwd /app/web/themes/custom/dagplejelager_theme install
docker-compose run node yarn --cwd /app/web/themes/custom/dagplejelager_theme encore dev
docker-compose run node yarn --cwd /app/web/themes/custom/dagplejelager_theme encore dev --watch
docker-compose run node yarn --cwd /app/web/themes/custom/dagplejelager_theme encore prod
```
